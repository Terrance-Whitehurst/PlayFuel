session: moh9uivm3dxv  # (prev: mogfjh9hslqr)
date: "2026-04-27"

project_context:
  name: PlayFuel
  stack: FastAPI (Python 3.11+), Pydantic v2, supabase-py, PyJWT, pytest
  api_root: apps/api/src/playfuel_api/
  rules_version: "1.1.0"  # bumped for doubles-spec nested SCENARIO_DURATIONS_MIN

domain_restrictions:
  write: .pi/multi-team/ only
  read: apps/, .pi/multi-team/, project root
  note: "QA Engineer CANNOT write to apps/. Must stage files in session dir and
         flag for Engineering/Lead to place them on disk."

task5_qa_pass:
  session: mogfjh9hslqr
  status: COMPLETE (QA pass) / BLOCKED (file write)
  missing_file_staged_at: ".pi/multi-team/sessions/mogfjh9hslqr/test_routes_smoke_READY_TO_WRITE.py"
  write_blocker: "QA domain is read-only on apps/; staged for Engineering to write"

invariant_matrix:
  - id: 1
    name: camelCase API output
    status: PASS
    evidence: "routes/plans.py: response_model_by_alias=True on @router.post('/{tid}/plans/generate');
               plan_dict = plan.model_dump(by_alias=True, mode='json') before INSERT"
  - id: 2
    name: test_gap_120_is_quick_pickup exists
    status: PASS
    evidence: "tests/test_buckets.py: function test_gap_120_is_quick_pickup() asserts
               food_bucket_for(120)==FoodBucket.quick_pickup AND pickup_bucket_for(120)==PickupBucket.wait_until_end"
  - id: 3
    name: half-open intervals correct
    status: PASS
    evidence: "rules/buckets.py: all comparisons use strict < (not <=);
               FOOD_BUCKET_BOUNDARIES=[45,90,150], PICKUP_BUCKET_BOUNDARIES=[60,120] confirmed in constants.py"
  - id: 4
    name: gap_status semantics
    status: PASS
    evidence: "rules/scenarios.py: if gap_minutes < 0 → overrun; GapStatus.tight if gap_minutes < TIGHT_GAP_THRESHOLD_MIN (30) else GapStatus.ok;
               no_next_match at top of _make_scenario when next_match is None"
  - id: 5
    name: overrun clamps
    status: PASS
    evidence: "rules/scenarios.py overrun branch: food=FoodBucket.bag_only, pickup=PickupBucket.bring_portable,
               rewarm_up=None, HTTP 200 always (generate_plan returns GeneratePlanResponse)"
  - id: 6
    name: extreme_heat_risk formula
    status: PASS
    evidence: "rules/weather.py line: flag_extreme_heat_risk: bool = flag_very_hot or (flag_hot and flag_humid)"
  - id: 7
    name: RLS-only ownership
    status: PASS
    evidence: "grep found zero eq('user_id',...) in routes/; only eq('id', str(user_id)) in /v1/me
               (fetching user's own row, not an ownership filter on a resource).
               db.py: client.postgrest.auth(creds.credentials) ✅"
  - id: 8
    name: DRAFT OQ flags preserved
    status: PASS
    evidence: "constants.py: [DRAFT — OQ-E] on TIGHT_GAP_THRESHOLD_MIN, [DRAFT — OQ-C] on REWARM_UP_OFFSET_MIN/DURATION_MIN;
               hard_coded_strings.py: OQ-11 PRE-LAUNCH BLOCKER on HEAT_EMERGENCY_TEXT;
               scenarios.py: TIGHT_GAP_THRESHOLD_MIN = 30 [DRAFT — OQ-E] in comment;
               plan.py: ⚠️ HEAT_EMERGENCY_TEXT is DRAFT — OQ-11 pre-launch blocker.
               OQ-A not found in any file (may have been in a different foundation file not shipped)."
  - id: 9
    name: scenario 5 xfail
    status: PASS
    evidence: "tests/test_scenarios.py: @pytest.mark.xfail(reason='OQ-F deferred to Phase 4')
               on test_scenario_5_rain_delay_engine_path()"
  - id: 10
    name: no LLM imports
    status: PASS
    evidence: "grep for openai/anthropic/litellm across apps/api/src/ → zero matches"
  - id: 11
    name: no prohibited phrases
    status: PASS
    evidence: "grep for all 8 prohibited phrases across apps/api/src/ → zero matches;
               hard_coded_strings.py text reviewed manually — no causal claims"
  - id: 12
    name: reuses existing models
    status: PASS
    evidence: "scenarios.py: from playfuel_api.models.api import FoodStrategy,OverrunWarning,PickupStrategy,RewarmUp,ScenarioPlan;
               plan.py: from playfuel_api.models.api import Plan, ScenarioPlan;
               routes/plans.py: from playfuel_api.models.api import GeneratePlanResponse; no parallel defs"

findings:
  - category: BRIEF_DEFECT
    severity: medium
    title: "Brief specifies 401 for no-token path, actual behaviour is 403"
    detail: "FastAPI's HTTPBearer(auto_error=True) raises HTTP 403 (not 401) when no
             Authorization header is present. 401 is raised only by verify_supabase_jwt
             when a token IS present but invalid/expired. The smoke test function was
             renamed test_protected_route_without_token_returns_403 and asserts 403.
             The brief's expected status code should be corrected to 403."

  - category: SECURITY
    severity: low
    title: "jwt.InvalidTokenError detail reflected in 401 response body"
    detail: "auth.py: detail=f'Invalid token: {exc}' reflects the pyjwt exception
             message into the API response. May leak token structure hints to attackers
             probing with malformed tokens. Recommend: replace with generic 'Invalid token'
             without the exception detail."
    file: "apps/api/src/playfuel_api/auth.py"

  - category: UNUSED_IMPORT
    severity: low
    title: "PlayerProfileRow imported but unused in player_profiles.py"
    detail: "routes/player_profiles.py imports PlayerProfileRow from playfuel_api.models.db
             but none of the route handlers use it. Handlers return dict[str,Any] from
             Supabase directly. No runtime impact; lint warning only."
    file: "apps/api/src/playfuel_api/routes/player_profiles.py"

  - category: TEST_GAP
    severity: medium
    title: "no_next_match path (§G.5) has no dedicated test"
    detail: "All 5 SCENARIO_ACCEPTANCE cases supply a next_match. The no_next_match
             branch in _make_scenario (§G.5) — food_strategy=None, pickup bucket=None,
             gap_minutes=None, gap_status=no_next_match — is not exercised by any test
             in test_scenarios.py."

  - category: TEST_GAP
    severity: low
    title: "build_plan_envelope heat_emergency_text path untested"
    detail: "plan.py: the branch that sets heat_emergency_text when
             weather_flags['flag_extreme_heat_risk'] is True is not covered by any test.
             Only unit-testable path not reached by existing suite."

  - category: TEST_GAP
    severity: low
    title: "derive_schedule_confidence not unit-tested directly"
    detail: "plan.py: derive_schedule_confidence() is only exercised indirectly via
             generate_plan route. No standalone unit test for low/medium/high branches."

  - category: TEST_GAP
    severity: low
    title: "rewarm_up boundary (gap=59 vs gap=60) not explicitly tested"
    detail: "REWARM_UP_MIN_GAP=60 means rewarm_up is non-null only when gap >= 60.
             No test directly asserts rewarm_up=None for gap=59 and non-None for gap=60
             in isolation (covered implicitly in scenario acceptance but not as a named
             boundary test)."

  - category: REGRESSION_CHECK
    severity: info
    title: "No regressions found vs foundation files"

new_oqs:
  - id: "OQ-QA-1"
    title: "HTTPBearer returns 403 not 401 for missing auth header — brief specifies 401"
    status: open
    priority: medium
    detail: "The TaskSpec and smoke test brief both say 'protected route w/o token → 401'.
             Actual: FastAPI HTTPBearer(auto_error=True) returns 403. Options:
             (a) Accept 403 as correct, update brief/docs;
             (b) Switch to HTTPBearer(auto_error=False) + custom dep that raises 401.
             Current test_routes_smoke.py asserts 403 (matching implementation)."

  - id: "OQ-QA-2"
    title: "PlayerProfileRow import unused in player_profiles.py — cleanup needed?"
    status: open
    priority: low
    detail: "Unused import raises linting warnings. Recommend removing before Phase 3."

test_coverage_patterns:
  total_as_of_doubles_delivery: "231 passed, 1 xfailed (was 180+1; +51 over doubles delegate)"
  total_as_of_session_moh9uivm3dxv: "180 passed, 1 xfailed (was 88+1 at session start; +92 over 4 deliveries)"
  bucket_tests: 13 cases (8 food + 4 pickup + 1 named regression)
  weather_tests: 6 parametrized cases (2 extreme_heat paths, 1 cold, 1 rain, 1 hot-only, 1 none)
  scenario_tests: 4 acceptance cases fully passing + 1 xfail (rain delay OQ-F)
  routes_smoke: 4 cases (healthz 200, no-token 401, mocked-auth 200, food_options_not_null)
  food_tests: test_food_categorize + test_food_templates + test_food_assembly + test_places_mock (all PASS)
  llm_tests: test_llm_template (10) + test_llm_safety (8) + test_llm_factory (5) + test_llm_routes (4) = 27 new
  eval_harness:
    runnable_as: "PYTHONPATH=src .venv/bin/python3.12 -m playfuel_api.eval.run_acceptance"
    note: "bare python3.12 -m ... fails without venv activation; use PYTHONPATH=src + venv python"
    fixtures: 5 scenarios, scenario 5 xfail per OQ-F
  still_not_covered:
    - derive_schedule_confidence unit tests (not in brief scope)
    - build_timeline() meal/warmUp/wakeUp event types (v1 only emits match+matchEnd)
  resolved_gaps:
    - no_next_match path: NOW COVERED
    - build_plan_envelope heat text: NOW COVERED
    - rewarm_up gap boundary isolation: NOW COVERED
    - verify_supabase_jwt negative paths: NOW COVERED

doubles_validation_report_written:
  path: .pi/multi-team/sessions/moh9uivm3dxv/doubles-validation-report.md
  verdict: "Demo-ready YES — 231 passed / 1 xfailed / 0 blockers"
  minor_deviation: "plans.match_type nullable vs NOT NULL DEFAULT 'singles' in spec (OQ-DBL-7)"
  new_security_findings: "SEC-6 (_CANONICAL_DURATIONS not updated for doubles) + SEC-7 (format not Literal-typed)"
  pre_record_actions: 3 (unchanged from Session 4 — LLM_PROVIDER, timeline, DEVELOPMENT_TEAM)
  consolidated_from: [doubles-qa-findings.md, doubles-security-findings.md, demo-readiness-report.md]

demo_readiness_report_written:
  path: .pi/multi-team/sessions/moh9uivm3dxv/demo-readiness-report.md
  verdict: "Demo-ready YES — 2 pre-record actions"
  blockers: none
  pre_record_actions:
    - "Set LLM_PROVIDER=template in apps/api/.env"
    - "Resolve timeline event count (extend build_timeline OR narrate around 3-event limit)"
    - "Fill DEVELOPMENT_TEAM in project.yml for on-device run"

profile_popout_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  verdict: Clean pass — 0 blockers
  test_count: '291 passed, 1 xfailed — UNCHANGED'
  dark_mode_continuity: PASS — @AppStorage("appearance_mode") key identical in SettingsView:13 and PlayFuelApp:37
  profile_button:
    dashboard: 'TournamentDashboardView:35/71/84 — showProfile state + person.crop.circle.fill + ProfileMenuSheet sheet'
    list: 'TournamentListView:12/51/65 — same pattern'
    existing_items_preserved: 'Add Match (dashboard :62) and + create tournament (list :45) both coexist with profile button'
  disclaimer_cleaned: 'DisclaimerView — 0 hits for appearance_mode/AppearanceMode/Picker. userDisclaimer+heatEmergencyText+prohibitedClaims all intact verbatim'
  new_files:
    ProfileMenuSheet: '4 pbxproj hits (PBXFileReference+PBXBuildFile+group+Sources)'
    SettingsView: '4 pbxproj hits'
  assets_hotfix_preserved: 'Assets.xcassets = 4 pbxproj hits; PBXResourcesBuildPhase lines 392-401; UUIDs AA0000AA0000AA0000AA0001-0004'
  xcode_xgen_1_root_cause: 'CONFIRMED xcodegen 2.45.4 bug — project.yml has correct type: folder.assetcatalog but xcodegen still emits no PBXResourcesBuildPhase. Manual 5-site UUID patch must be re-applied after every xcodegen generate.'
  key_patterns:
    - 'Two ToolbarItem(.topBarTrailing) items coexist in a single .toolbar block — SwiftUI renders both; second registered appears left of first'
    - 'OQ-XCG-1 is a permanent recurring fix requirement until xcodegen is upgraded'
    - 'Profile popout pattern: ProfileMenuSheet (2-row List) → SettingsView (Form) — both use @Environment(\.dismiss) + Done toolbar button'
    - 'DisclaimerView post-cleanup: only HardCodedStrings.userDisclaimer + heatEmergencyText + prohibited claims. No Picker, no @AppStorage.'

dark_mode_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '291 passed, 1 xfailed — UNCHANGED (no backend touched)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  safety_gate: PASS — EmergencyStrip Color.red+.white literal; EmergencyBanner Color.red.gradient+.white literal
  color_audit_greps:
    Color_white_black: '1 hit — PlanSummaryCard.swift:62 in #else/#if os(iOS) macOS branch — expected, iOS path uses Color(.systemBackground)'
    Color_red: 0 hits
    Color_gray: 0 hits
    hex_literals: 0 hits
    dark_preview_blocks: 23 files confirmed
  asset_catalog:
    on_disk: 'AccentColor.colorset + PlayFuelGreen.colorset both present with light+dark variants'
    light_accent: 'R=0.133 G=0.498 B=0.322 (#227F52)'
    dark_accent: 'R=0.239 G=0.722 B=0.494 (lighter/more saturated)'
    pbxproj_issue: 'grep -c Assets.xcassets project.pbxproj = 0; NO PBXResourcesBuildPhase in target buildPhases'
    impact: 'Color.accentColor = system blue at runtime (not PlayFuel green); Color("PlayFuelGreen") = nil. Semantic system colors unaffected.'
  appearance_picker:
    app_storage_key: appearance_mode
    enum: 'AppearanceMode — .system/.light/.dark in PlayFuelApp.swift'
    apply_site: '.preferredColorScheme(activeColorScheme) on WindowGroup root'
    ui: 'Segmented picker in DisclaimerView — USER_DISCLAIMER verbatim ABOVE, picker in own section below Divider'
  new_findings:
    - id: QA-DM-1
      severity: minor
      title: 'Assets.xcassets not in PBXResourcesBuildPhase — brand colors may not render at runtime'
      detail: 'project.yml has resources: optional:true but xcodegen generated no PBXResourcesBuildPhase. Color.accentColor defaults to system blue. Fix: drag Assets.xcassets into target Resources build phase in Xcode, or fix project.yml resources spec to force inclusion.'
  key_patterns:
    - 'EmergencyStrip/Banner: always use literal Color.red (NOT Color(.systemRed)) + literal .white foreground. Never swap for semantic variants.'
    - 'Color.white in #else/#if os(iOS) branch is acceptable — only compiled on macOS, not iOS. Report as expected.'
    - '#if os(iOS) pattern: always check both branches of conditional compilation in grep results'
    - 'xcodegen optional:true resources: if path exists xcodegen SHOULD include it, but may generate no PBXResourcesBuildPhase. Always verify with grep -c "Assets.xcassets" project.pbxproj after regen.'
    - 'Asset catalog colors require PBXResourcesBuildPhase to be compiled — Color.accentColor without it falls back to system blue'

timeline_timestamps_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '291 passed, 1 xfailed — UNCHANGED (no backend touched)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  fix_confirmed: 'TimelineView.swift:42 — Text(event.time.asClockTimeFromISO) — raw ISO gone'
  dual_form_contract: 'asClockTimeFromISO returns self on parse failure — FakeData sentinels pass through unchanged'
  sentinel_strings_confirmed:
    - 'FakeData:316 — "6:00 AM"'
    - 'FakeData:330 — "8:00 AM"'
    - 'FakeData:358 — "During Match"'
    - 'FakeData:365 — "~11:00 AM"'
    - 'FakeData:393 — "Post-Match"'
  grep_audits: all 3 clean (0 raw ISO, 0 .description on dates, only 1 .time hit = the fixed line)
  pbxproj: 4 hits for DateFormatting.swift
  sister_formatter:
    nextaction: 'Already uses DurationFormatting.friendly — no DateFormatter duplication'
    schedulestrip: 'Local ISO8601DateFormatter + DateFormatter(dateFormat: h:mm a) at lines 141-151 — correct but duplicates DateFormatting.clockTime(). OQ-TS-1 post-demo polish.'
  new_oqs:
    - id: OQ-TS-1
      severity: cosmetic
      title: 'ScheduleStripView.timeString uses local DateFormatter pair — duplicate of DateFormatting.clockTime()'
      detail: 'Correct output, just not using shared helper. Post-demo consolidation.'
  key_pattern: 'For String timestamp fields on models: asClockTimeFromISO two-fallback pattern (withInternetDateTime + default ISO8601 + return self) is the correct approach when FakeData has mixed ISO/human-readable strings on the same field.'

food_deck_map_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '291 passed, 1 xfailed (was 277+1; +14 new in test_food_suggestions.py)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  spec_compliance: '16/17 §G items PASS; 1 ⚠️ (README dashboard table stale — cosmetic)'
  pbxproj: '4 hits x 4 new files = 16 total entries confirmed'
  dto_shim_removed: 'CONFIRMED — FoodOptionDTO.driveTimeMinutes is Int? with nil pass-through; no ?? 0'
  starbucks_confirmed: 'CONFIRMED — FakeData.dallasFoodOptions[3]; mainOptions[0] = "Oatmeal"; isDraft=true'
  new_findings:
    - id: QA-FDM-1
      severity: minor
      title: 'README dashboard order table stale — FoodCardView at row 5 should be FoodOptionDeck; 2 bubbles should be 3'
      detail: 'New files table (lines 80-82) correct. Dashboard order table (line 260) unchanged from Phase 8.1. Pre-App Store cleanup.'
    - id: QA-FDM-2
      severity: cosmetic
      title: 'Open in Maps hidden (not disabled+subtext) when option.lat/lng nil'
      detail: 'Spec said disabled+subtext. Implementation hides button entirely. All Dallas fixtures have coords so no demo impact.'
    - id: QA-FDM-3
      severity: minor
      title: 'Food allergy disclaimer in FoodOptionDetailSheet.swift:130 is hardcoded, not from HardCodedStrings'
      detail: 'String: "If your player has food allergies…". No HardCodedStrings.foodAllergyDisclaimer constant. Add before App Store.'
  key_patterns:
    - 'Annotation (not Marker) is required for tap-to-drill on food map pins — Marker has no tap action in SwiftUI Map'
    - '(0,0) sentinel for missing Tournament venue coords is the correct nil-handling pattern (Tournament.lat/lon are non-optional Double)'
    - 'Empty conditional sections must guard BOTH the header label AND the list — if header rendered without content, spec fails'
    - 'FoodOption.Identifiable requires id: UUID — verified in Models/FoodOption.swift; required for .sheet(item:) and ForEach'

duration_format_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '277 passed, 1 xfailed (was 262+1; +15 new in test_duration_format.py)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  algorithm_correct: true — both Python (duration_format.py) and Swift (DurationFormatting.swift) match spec table verbatim
  ios_grep_audits: all 4 pass (0/0/0 bare-min hits; 8 DurationFormatting.friendly hits >= 4 required)
  sites_converted: '5 ScenarioCardView + 1 MatchCreateView + 1 NextActionCard + 1 FoodCardView = 8 iOS sites; 2 backend (llm.py + plan.py)'
  safety_regex_regression: 'PASS — _DURATION_RE handles both legacy ("75 min") and friendly ("1 hr 15 min") forms; all 10 runtime smoke cases correct'
  format_spec_table: '13/14 explicitly tested; 119 min trivially derivable (cosmetic gap)'
  strings_left_alone:
    - 'next_action.py: "Refuel within 30 minutes." — natural prose'
    - 'ScheduleStripView: Date-formatted time strings, not minute counts'
  new_findings:
    - id: QA-DF-1
      severity: minor
      title: 'test_llm_safety.py missing friendly-form duration test cases'
      detail: 'No test for "1 hr 15 min" passing validation or "1 hr 17 min" being rejected. Logic is correct (runtime verified). Add 2 parametrized cases pre-TestFlight.'
    - id: QA-DF-2
      severity: cosmetic
      title: '119 min case ("1 hr 59 min") not explicit in test_duration_format.py'
      detail: 'Trivially derivable from algorithm. Non-blocking.'
  lesson_captured: 'When a regex is updated to handle dual input forms, always check test coverage for BOTH forms — not just the one the existing tests happened to use.'

header_bubbles_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '262 passed, 1 xfailed — UNCHANGED (backend untouched)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  qa_ia_1_fix: CONFIRMED — EmergencyStrip at line 116 < hasBothTypes Picker at line 121
  inline_removals: all 3 greps return 0 hits (PlanSummaryCard, WeatherCardView, EmergencyBanner)
  new_files_confirmed: all 6 on disk (62/81/66/98/39/56 lines)
  critical_finding: 'PlanSummarySheet correctly renders only PlanExplanation fields.
    scenarioExplanations DOES exist on model (Planning wrong) but intentionally skipped
    (ScenarioCardView is correct surface). No compile-fail risk.'
  heat_guidance_verbatim: 'HeatGuidanceSheet uses ONLY HardCodedStrings.heatEmergencyText
    and .userDisclaimer — zero manual string literals — confirmed at lines 25 and 35'
  emergency_strip_safety: 'EmergencyStrip @State sheetShown controls ONLY the sheet
    presentation. Strip visibility driven entirely by parent conditional
    plan.weather.extremeHeatRisk — cannot be suppressed by any user UI action'
  field_name_correction: 'WeatherSnapshot uses flags: [WeatherFlag] enum array NOT flat
    booleans. Always use weather.flags.contains(.xxx) not weather.flagXxx'
  new_findings:
    - id: QA-HB-6
      severity: cosmetic
      title: PlanSummarySheet init is non-optional — nil gate at call site
    - id: QA-HB-7
      severity: cosmetic
      title: explanation.model + explanation.generatedAt not rendered in sheet (not spec-required)

new_oqs_session_moh9uivm3dxv:
  - id: OQ-QA-3
    title: "Timeline event count: build_timeline() v1 = 3 events, demo script claims 12+"
    status: open
    priority: minor
    detail: "build_timeline() generates 1 match_start + 1 matchEnd (match 1 only) + 1 match_start per match = 3 events
             for Dallas seed. FakeData.dallasTimeline has 12 events but is now preview-only (splice retired).
             Demo step 10 will show 3 events live. Code comment: 'Future versions will splice meal/warmUp/wakeUp'."
  - id: OQ-QA-4
    title: "Broad except Exception around LLM block in plans.py catches programming errors"
    status: open
    priority: minor
    detail: "noqa: BLE001 suppresses lint. Not silent (logs WARNING + exc_info=True). Post-demo: narrow scope."
  - id: OQ-QA-5
    title: "validate_explanation check #4 restaurant-hallucination is a pass for non-empty food lists"
    status: open
    priority: cosmetic
    detail: "Only real guard is the 'no key → TemplateProvider' architecture. Needs strengthening for real LLM providers."
  - id: OQ-QA-6
    title: "LLM_PROVIDER=template must be set in .env if ANTHROPIC_API_KEY is live in shell"
    status: open
    priority: minor
    detail: "auto mode routes to AnthropicProvider stub (NotImplementedError), caught by broad except, llmSummary=null.
             Demo operator must set LLM_PROVIDER=template in apps/api/.env before recording."

nutrition_ia_validation:
  session: moh9uivm3dxv
  status: COMPLETE
  test_count: '256 passed, 1 xfailed (was 231+1; +25 over IA pivot delegate)'
  eval_harness: exit 0, 4 PASS, 1 XFAIL
  findings:
    - id: QA-IA-1
      severity: minor
      title: 'EmergencyBanner/Picker render order swapped when hasBothTypes=true'
      detail: 'Picker in envelopeContent() lines 103-112 renders BEFORE planContent() call.
               EmergencyBanner is first element of planContent (line 176). When hasBothTypes=true,
               Picker appears at visual #0, EmergencyBanner at visual #1 — inverted from spec #0/#1.
               Demo-safe: Dallas seed is singles-only, Picker never renders. Safety data path intact.'
    - id: OQ-IA-9 (carry)
      severity: minor
      title: 'INSERT not upsert in plans.py — re-generate will DB-constraint-fail'
      detail: 'routes/plans.py:263 uses .insert() not .upsert(). Docstring at line 11 says
               upsert — inaccurate. Partial unique index rejects duplicate (match_id, match_type).
               Non-blocking for demo; UX bug for TestFlight re-generates.'
  card_order_verified:
    planContent_line_176: EmergencyBanner
    planContent_line_181: PlanSummaryCard
    planContent_line_185: ScheduleStripView
    planContent_line_192: NextActionCard
    planContent_line_195: FoodCardView
    planContent_line_200: scenariosSection
    planContent_line_202: Timeline button
    planContent_line_217: 'WeatherCardView(compact: true)'
    planContent_line_220: footerDisclaimer
  new_oqs:
    - OQ-IA-W1: 'WeatherPill omits apparent temp (feels like) — cosmetic'
    - OQ-IA-6: 'minsUntil stale after server generation time; client-side recompute is Phase 8+'

ia_pivot_patterns:
  - 'When checking card order: differentiate envelopeContent (wraps planContent) vs planContent itself.
     Items in envelopeContent render ABOVE items in planContent even if planContent"s comment says #0.'
  - 'Picker/EmergencyBanner swap: only triggered when hasBothTypes=true AND extreme_heat_risk=true — rare production scenario but spec-required fix.'
  - 'compact: Bool = false init param is the correct pattern for demoting a view component without removing it.'
  - 'Per-match plan loop (vs first-of-type): key correctness check is that match_rows order (display_order ASC, scheduled_start ASC) feeds straight into the response arrays without re-sort.'
  - 'INSERT vs upsert: always grep for .upsert vs .insert when a unique constraint is added in the same session. Docstring claims upsert; implementation has insert — a documentation drift bug.'

doubles_qa_patterns:
  - "For envelope-refactor validations: grep both the bare old name AND the new name (e.g. currentPlan vs currentPlanEnvelope)"
  - "partnerCoordination uses camelCase value (consistent with ALL other TimelineEventKind values) — spec snake note was a naming suggestion not a mandate"
  - "Nullable migration column vs NOT NULL DEFAULT: verify routes always insert explicit value; null-only for pre-migration legacy rows"
  - "Dual pickers in form views: verify both always-visible (type) and conditional (format) pickerStyle(.segmented) present"
  - "resolveActivePlan helper pattern: @ViewBuilder bodies cannot contain let bindings on macOS toolchain — extract to function"

demonstrated_patterns:
  - "eval harness runnable via: PYTHONPATH=src .venv/bin/python3.12 -m playfuel_api.eval.run_acceptance"
  - "QA domain cannot write to apps/ — stage in session dir or report inline"
  - "iOS source truth: splice retired means dallasTimeline (FakeData) is preview-only; live timeline comes from API"
  - "RLS column inheritance: ALTER TABLE ADD COLUMN is covered by existing table-level RLS policies automatically"

write_domain_reminder:
  note: "QA Engineer CANNOT write to apps/ directly. Always stage in session dir."
  path_confirmed: "Direct write to apps/ returns 'Domain: write denied' error."
  staging_location: ".pi/multi-team/sessions/mogfjh9hslqr/staged_validation/"
  target_mapping:
    tests: "staged_validation/tests/* → apps/api/src/playfuel_api/tests/*"
    eval: "staged_validation/eval/* → apps/api/src/playfuel_api/eval/*"
    readme: "staged_validation/README_APPEND.md → append to apps/api/README.md"
